import React from "react";

//keep your code separated into different sheets for organizational purposes.
const Greetings = ({firstName, lastName}) => (
    <div>
    Hey you! {firstName} {lastName}
    </div>
);
//function should be exported at the bottom of the sheet. 
export default Greetings;